extends ../template/layout

block content
  .row
    .small-12.columns
      #container(data-postcard-id='#{postcard.id}')
        h1 Southern Greetings
        .row
          .small-6.columns
            h4.slogan Seasons Greetings, Southern Style
          .small-6.columns
        .row
          .small-12.columns
            if postcard.state.isLandscape
              #front-card(style='background: url(/images/backgrounds/#{postcard.background}.jpg);')
                h2(style='color: #{postcard.frontFontColor}; font-size: #{postcard.frontFontSize};') Greetings from
                #pc-state(style='width: #{postcard.state.width}px; height: #{postcard.state.height}px; background: url(/images/states/#{postcard.state.img}) no-repeat; margin: 0 auto;')
                  .final-flag(style='background: url(/images/flags/#{postcard.flag.img}.png) no-repeat; background-size: 100%; top: #{postcard.flag.top}; left: #{postcard.flag.left};')
                h2(style='color: #{postcard.frontFontColor};  font-size: #{postcard.frontFontSize};') #{postcard.city}, #{postcard.state.name}
            else
              #front-card(style='width: 500px; height: 700px; background: url(/images/backgrounds/#{postcard.background}.jpg);')
                h2(style='color: #{postcard.frontFontColor};  font-size: #{postcard.frontFontSize};') Greetings from
                #pc-state(style='width: #{postcard.state.width}px; height: #{postcard.state.height}px; background: url(/images/states/#{postcard.state.img}) no-repeat; margin: 0 auto;')
                  .final-flag(style='background: url(/images/flags/#{postcard.flag.img}.png) no-repeat; background-size: 100%; top: #{postcard.flag.top}; left: #{postcard.flag.left};')
                h2(style='color: #{postcard.frontFontColor};  font-size: #{postcard.frontFontSize};') #{postcard.city}, #{postcard.state.name}
        .row
          .small-12.columns
            #back-card
              #back-img-container
                img(src='/uploads/#{postcard.id}/#{postcard.backImg}')
              #back-img-message
                h4(style='color: #{postcard.backFontColor}') #{postcard.backGreeting} from #{postcard.backFamilyName}
        .row
          .small-3.small-offset-3.columns
            form(action='/postcards/#{postcard.id}', method='post')
                  input(type='hidden', name='_method', value='delete')
                  button#delete.alert.radius.button.expand(type='submit') Delete
          .small-3.columns
            button#print.button.radius.expand Print
          .small-3.columns

